[project]
name = "edge-tail-logging-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=9.0.3",
  "tqdm>=4.66.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
