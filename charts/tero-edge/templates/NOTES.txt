Tero Edge DaemonSet installed.

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status daemonset/{{ include "tero-edge.fullname" . }}

Inspect rendered config:
  kubectl -n {{ .Release.Namespace }} get configmap {{ include "tero-edge.fullname" . }}-config -o yaml

{{- if .Values.service.enabled }}
Service is enabled as {{ .Values.service.type }} on port {{ .Values.service.port }}.
{{- else if .Values.container.hostPort.enabled }}
Service is disabled. Traffic is expected via hostPort {{ .Values.container.hostPort.port }}.
{{- else }}
Service and hostPort are both disabled. Create a Service or enable hostPort to route traffic to tero-edge.
{{- end }}
